manifestVersion: 1
name: "@dougschaefer/openai-usage"
version: "2026.06.08.1"
description: "OpenAI platform usage and cost reporting via the Organization Usage API — daily and total completion token counts and USD spend, month-to-date by default. Vault-based Admin API key (the api.usage.read scope is required; a standard project key cannot read usage)."
repository: https://github.com/dougschaefer6/swamp-openai-usage
additionalFiles:
  - README.md
  - LICENSE.txt
labels:
  - openai
  - usage
  - cost
  - tokens
  - finops
  - monitoring
models:
  - openai-usage/usage.ts
